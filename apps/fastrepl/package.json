{
  "name": "@hypr/fastrepl",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tailwindcss -i ./src/input.css -o ./dist/output.css --watch",
    "build": "mkdir -p dist && cp index.html dist/index.html && (cp -r public/. dist/ 2>/dev/null || true) && tailwindcss -i ./src/input.css -o ./dist/output.css --minify",
    "typecheck": "echo 'no typecheck'"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.0",
    "tailwindcss": "^4.1.0"
  }
}
