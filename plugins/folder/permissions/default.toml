[default]
description = "Default permissions for the plugin"
permissions = [
    "allow-ping",
    "allow-move-session",
    "allow-create-folder",
    "allow-rename-folder",
    "allow-delete-folder",
]
