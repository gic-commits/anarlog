[package]
name = "db-app"
version = "0.1.0"
edition = "2024"

[dependencies]
hypr-db-core2 = { workspace = true }
sqlx = { workspace = true, features = ["runtime-tokio", "sqlite", "sqlite-unbundled"] }
